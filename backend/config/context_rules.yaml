rules:
  brain_dump:
    load:
      - profile
      - recent_messages
    optional:
      - entities
      - graph_context

  query_next:
    load:
      - profile
      - open_items
      - urgent_items
      - recent_messages
    optional:
      - calendar_events
      - memories
      - graph_context

  query_search:
    load:
      - profile
      - recent_messages
    optional:
      - graph_context

  query_upcoming:
    load:
      - profile
      - urgent_items
    optional:
      - graph_context

  status_done:
    load:
      - profile
      - open_items
      - recent_messages
    optional:
      - graph_context

  status_cant:
    load:
      - profile
      - open_items
      - recent_messages
    optional:
      - graph_context

  emotional:
    load:
      - profile
      - recent_messages
    optional:
      - memories
      - graph_context

  onboarding:
    load:
      - profile

  meta:
    load:
      - profile

  conversation:
    load:
      - profile
      - recent_messages
    optional:
      - memories
      - entities
      - graph_context

defaults:
  recent_messages_limit: 20
  open_items_limit: 50
  memories_limit: 5
