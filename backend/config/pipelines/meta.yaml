name: meta
description: User asks about the app itself

steps:
  - id: respond
    type: llm_call
    prompt: meta_respond.md
    model: null
    stream: true
    input:
      message: "${user_message}"
