name: onboarding
description: First message from new user

steps:
  - id: respond
    type: llm_call
    prompt: onboarding_respond.md
    model: null
    stream: true
    input:
      message: "${user_message}"
